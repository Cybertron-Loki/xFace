<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ming.test.xface.dao.UserDao">

    <select id="getUserById" parameterType="java.lang.Integer" resultType="ming.test.xface.enity.pojo.User">
        select *
        from user
        where id = #{id}
          and isDelete = 0;
    </select>

    <insert id="addUser" parameterType="ming.test.xface.enity.pojo.User">
        <choose>
            <when test="avatar != null">
                INSERT INTO user (username, nickname, account, password, avatar)
                VALUES (#{username}, #{nickname}, #{account}, #{password}, #{avatar});
            </when>
            <otherwise>
                INSERT INTO user (username, nickname, account, password)
                VALUES (#{username}, #{nickname}, #{account}, #{password});
            </otherwise>
        </choose>
    </insert>

</mapper>